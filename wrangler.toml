# Cloudflare Pages build config
# Build command + output dir must both be set so Pages runs build (not skipped)
name = "teazr"
pages_build_output_dir = "./dist/public"
pages_build_command = "npm install && npm run build"
